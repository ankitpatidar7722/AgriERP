# React 19 is newer than several Radix packages' declared peer ranges. They work
# fine; npm's strict peer resolution just refuses to install them. Setting this
# here keeps installs non-interactive and reproducible for everyone on the team.
legacy-peer-deps=true
