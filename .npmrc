# React 19 has peer-dependency conflicts with some libs (indas-ui, etc.), so
# installs must use legacy peer resolution. This makes `npm install` on Vercel /
# CI / Docker behave the same as local without extra flags.
legacy-peer-deps=true
